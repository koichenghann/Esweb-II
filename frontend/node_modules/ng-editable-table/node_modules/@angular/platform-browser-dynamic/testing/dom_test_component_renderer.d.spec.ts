import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DomTestComponentRendererDComponent } from './dom_test_component_renderer.d.component';

describe('DomTestComponentRendererDComponent', () => {
    let component: DomTestComponentRendererDComponent;
    let fixture: ComponentFixture<DomTestComponentRendererDComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [DomTestComponentRendererDComponent]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(DomTestComponentRendererDComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});