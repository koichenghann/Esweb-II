import { TestBed, inject } from '@angular/core/testing';

import { DomSanitizationServiceDService } from './dom_sanitization_service.d.service';

describe('DomSanitizationServiceDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DomSanitizationServiceDService]
    });
  });

  it('should be created', inject([DomSanitizationServiceDService], (service: DomSanitizationServiceDService) => {
    expect(service).toBeTruthy();
  }));
});