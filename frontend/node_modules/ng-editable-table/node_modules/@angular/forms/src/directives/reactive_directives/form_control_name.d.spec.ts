import { FormControlNameDDirective } from './form_control_name.d.directive';

describe('FormControlNameDDirective', () => {
  it('should create an instance', () => {
    const directive = new FormControlNameDDirective();
    expect(directive).toBeTruthy();
  });
});
