import { NgControlDDirective } from './ng_control.d.directive';

describe('NgControlDDirective', () => {
  it('should create an instance', () => {
    const directive = new NgControlDDirective();
    expect(directive).toBeTruthy();
  });
});
