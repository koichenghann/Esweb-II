import { RangeValueAccessorDDirective } from './range_value_accessor.d.directive';

describe('RangeValueAccessorDDirective', () => {
  it('should create an instance', () => {
    const directive = new RangeValueAccessorDDirective();
    expect(directive).toBeTruthy();
  });
});
