import { NgFormDDirective } from './ng_form.d.directive';

describe('NgFormDDirective', () => {
  it('should create an instance', () => {
    const directive = new NgFormDDirective();
    expect(directive).toBeTruthy();
  });
});
