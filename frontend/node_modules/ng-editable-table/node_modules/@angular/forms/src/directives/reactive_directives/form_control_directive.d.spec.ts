import { FormControlDirectiveDDirective } from './form_control_directive.d.directive';

describe('FormControlDirectiveDDirective', () => {
  it('should create an instance', () => {
    const directive = new FormControlDirectiveDDirective();
    expect(directive).toBeTruthy();
  });
});
