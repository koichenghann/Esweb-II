import { FormGroupDirectiveDDirective } from './form_group_directive.d.directive';

describe('FormGroupDirectiveDDirective', () => {
  it('should create an instance', () => {
    const directive = new FormGroupDirectiveDDirective();
    expect(directive).toBeTruthy();
  });
});
