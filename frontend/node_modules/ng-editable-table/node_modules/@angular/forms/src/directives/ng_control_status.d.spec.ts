import { NgControlStatusDDirective } from './ng_control_status.d.directive';

describe('NgControlStatusDDirective', () => {
  it('should create an instance', () => {
    const directive = new NgControlStatusDDirective();
    expect(directive).toBeTruthy();
  });
});
