import { RadioControlValueAccessorDDirective } from './radio_control_value_accessor.d.directive';

describe('RadioControlValueAccessorDDirective', () => {
  it('should create an instance', () => {
    const directive = new RadioControlValueAccessorDDirective();
    expect(directive).toBeTruthy();
  });
});
