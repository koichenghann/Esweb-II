import { SelectControlValueAccessorDDirective } from './select_control_value_accessor.d.directive';

describe('SelectControlValueAccessorDDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectControlValueAccessorDDirective();
    expect(directive).toBeTruthy();
  });
});
