import { FormInterfaceDDirective } from './form_interface.d.directive';

describe('FormInterfaceDDirective', () => {
  it('should create an instance', () => {
    const directive = new FormInterfaceDDirective();
    expect(directive).toBeTruthy();
  });
});
