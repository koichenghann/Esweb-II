import { AbstractControlDirectiveDDirective } from './abstract_control_directive.d.directive';

describe('AbstractControlDirectiveDDirective', () => {
  it('should create an instance', () => {
    const directive = new AbstractControlDirectiveDDirective();
    expect(directive).toBeTruthy();
  });
});
