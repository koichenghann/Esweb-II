import { ControlValueAccessorDDirective } from './control_value_accessor.d.directive';

describe('ControlValueAccessorDDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlValueAccessorDDirective();
    expect(directive).toBeTruthy();
  });
});
