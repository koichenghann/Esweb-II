import { ValidatorsDDirective } from './validators.d.directive';

describe('ValidatorsDDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorsDDirective();
    expect(directive).toBeTruthy();
  });
});
