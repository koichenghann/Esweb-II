import { NormalizeValidatorDDirective } from './normalize_validator.d.directive';

describe('NormalizeValidatorDDirective', () => {
  it('should create an instance', () => {
    const directive = new NormalizeValidatorDDirective();
    expect(directive).toBeTruthy();
  });
});
