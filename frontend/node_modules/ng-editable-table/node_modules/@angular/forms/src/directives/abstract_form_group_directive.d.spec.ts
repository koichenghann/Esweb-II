import { AbstractFormGroupDirectiveDDirective } from './abstract_form_group_directive.d.directive';

describe('AbstractFormGroupDirectiveDDirective', () => {
  it('should create an instance', () => {
    const directive = new AbstractFormGroupDirectiveDDirective();
    expect(directive).toBeTruthy();
  });
});
