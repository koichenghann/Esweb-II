import { CheckboxValueAccessorDDirective } from './checkbox_value_accessor.d.directive';

describe('CheckboxValueAccessorDDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxValueAccessorDDirective();
    expect(directive).toBeTruthy();
  });
});
