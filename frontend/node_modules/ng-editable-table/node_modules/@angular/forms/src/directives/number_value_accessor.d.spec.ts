import { NumberValueAccessorDDirective } from './number_value_accessor.d.directive';

describe('NumberValueAccessorDDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberValueAccessorDDirective();
    expect(directive).toBeTruthy();
  });
});
