import { ErrorExamplesDDirective } from './error_examples.d.directive';

describe('ErrorExamplesDDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorExamplesDDirective();
    expect(directive).toBeTruthy();
  });
});
