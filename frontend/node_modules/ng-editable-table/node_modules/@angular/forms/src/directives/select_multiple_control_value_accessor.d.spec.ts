import { SelectMultipleControlValueAccessorDDirective } from './select_multiple_control_value_accessor.d.directive';

describe('SelectMultipleControlValueAccessorDDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectMultipleControlValueAccessorDDirective();
    expect(directive).toBeTruthy();
  });
});
