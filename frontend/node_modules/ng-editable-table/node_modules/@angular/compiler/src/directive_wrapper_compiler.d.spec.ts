import { DirectiveWrapperCompilerDDirective } from './directive_wrapper_compiler.d.directive';

describe('DirectiveWrapperCompilerDDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveWrapperCompilerDDirective();
    expect(directive).toBeTruthy();
  });
});
