import { PipeResolverMockDPipe } from './pipe_resolver_mock.d.pipe';

describe('PipeResolverMockDPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeResolverMockDPipe();
    expect(pipe).toBeTruthy();
  });
});
