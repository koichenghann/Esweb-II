import { NgTemplateOutletDDirective } from './ng_template_outlet.d.directive';

describe('NgTemplateOutletDDirective', () => {
  it('should create an instance', () => {
    const directive = new NgTemplateOutletDDirective();
    expect(directive).toBeTruthy();
  });
});
