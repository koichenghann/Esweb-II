import { IndexDDirective } from './index.d.directive';

describe('IndexDDirective', () => {
  it('should create an instance', () => {
    const directive = new IndexDDirective();
    expect(directive).toBeTruthy();
  });
});
