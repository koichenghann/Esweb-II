import { LowercasePipeDPipe } from './lowercase_pipe.d.pipe';

describe('LowercasePipeDPipe', () => {
  it('create an instance', () => {
    const pipe = new LowercasePipeDPipe();
    expect(pipe).toBeTruthy();
  });
});
