import { DatePipeDPipe } from './date_pipe.d.pipe';

describe('DatePipeDPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePipeDPipe();
    expect(pipe).toBeTruthy();
  });
});
