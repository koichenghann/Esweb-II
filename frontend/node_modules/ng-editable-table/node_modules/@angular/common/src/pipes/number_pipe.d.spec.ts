import { NumberPipeDPipe } from './number_pipe.d.pipe';

describe('NumberPipeDPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberPipeDPipe();
    expect(pipe).toBeTruthy();
  });
});
