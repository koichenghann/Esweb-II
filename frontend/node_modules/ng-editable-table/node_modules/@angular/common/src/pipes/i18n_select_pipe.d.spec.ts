import { I18nSelectPipeDPipe } from './i18n_select_pipe.d.pipe';

describe('I18nSelectPipeDPipe', () => {
  it('create an instance', () => {
    const pipe = new I18nSelectPipeDPipe();
    expect(pipe).toBeTruthy();
  });
});
