import { JsonPipeDPipe } from './json_pipe.d.pipe';

describe('JsonPipeDPipe', () => {
  it('create an instance', () => {
    const pipe = new JsonPipeDPipe();
    expect(pipe).toBeTruthy();
  });
});
