import { AsyncPipeDPipe } from './async_pipe.d.pipe';

describe('AsyncPipeDPipe', () => {
  it('create an instance', () => {
    const pipe = new AsyncPipeDPipe();
    expect(pipe).toBeTruthy();
  });
});
