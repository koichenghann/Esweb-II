import { TestBed, inject } from '@angular/core/testing';

import { SymbolsDService } from './symbols.d.service';

describe('SymbolsDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SymbolsDService]
    });
  });

  it('should be created', inject([SymbolsDService], (service: SymbolsDService) => {
    expect(service).toBeTruthy();
  }));
});