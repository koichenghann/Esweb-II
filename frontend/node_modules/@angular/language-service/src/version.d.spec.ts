import { TestBed, inject } from '@angular/core/testing';

import { VersionDService } from './version.d.service';

describe('VersionDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [VersionDService]
    });
  });

  it('should be created', inject([VersionDService], (service: VersionDService) => {
    expect(service).toBeTruthy();
  }));
});