import { TestBed, inject } from '@angular/core/testing';

import { DefinitionsDService } from './definitions.d.service';

describe('DefinitionsDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DefinitionsDService]
    });
  });

  it('should be created', inject([DefinitionsDService], (service: DefinitionsDService) => {
    expect(service).toBeTruthy();
  }));
});