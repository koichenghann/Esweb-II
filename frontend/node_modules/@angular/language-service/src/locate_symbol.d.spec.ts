import { TestBed, inject } from '@angular/core/testing';

import { LocateSymbolDService } from './locate_symbol.d.service';

describe('LocateSymbolDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [LocateSymbolDService]
    });
  });

  it('should be created', inject([LocateSymbolDService], (service: LocateSymbolDService) => {
    expect(service).toBeTruthy();
  }));
});