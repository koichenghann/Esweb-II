import { TestBed, inject } from '@angular/core/testing';

import { TsPluginDService } from './ts_plugin.d.service';

describe('TsPluginDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TsPluginDService]
    });
  });

  it('should be created', inject([TsPluginDService], (service: TsPluginDService) => {
    expect(service).toBeTruthy();
  }));
});