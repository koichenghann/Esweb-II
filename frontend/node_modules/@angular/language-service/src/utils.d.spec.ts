import { TestBed, inject } from '@angular/core/testing';

import { UtilsDService } from './utils.d.service';

describe('UtilsDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [UtilsDService]
    });
  });

  it('should be created', inject([UtilsDService], (service: UtilsDService) => {
    expect(service).toBeTruthy();
  }));
});