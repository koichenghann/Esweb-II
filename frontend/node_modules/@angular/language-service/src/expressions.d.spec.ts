import { TestBed, inject } from '@angular/core/testing';

import { ExpressionsDService } from './expressions.d.service';

describe('ExpressionsDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ExpressionsDService]
    });
  });

  it('should be created', inject([ExpressionsDService], (service: ExpressionsDService) => {
    expect(service).toBeTruthy();
  }));
});