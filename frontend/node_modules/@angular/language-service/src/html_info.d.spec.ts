import { TestBed, inject } from '@angular/core/testing';

import { HtmlInfoDService } from './html_info.d.service';

describe('HtmlInfoDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [HtmlInfoDService]
    });
  });

  it('should be created', inject([HtmlInfoDService], (service: HtmlInfoDService) => {
    expect(service).toBeTruthy();
  }));
});