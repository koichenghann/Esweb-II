import { TestBed, inject } from '@angular/core/testing';

import { ExpressionDiagnosticsDService } from './expression_diagnostics.d.service';

describe('ExpressionDiagnosticsDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ExpressionDiagnosticsDService]
    });
  });

  it('should be created', inject([ExpressionDiagnosticsDService], (service: ExpressionDiagnosticsDService) => {
    expect(service).toBeTruthy();
  }));
});