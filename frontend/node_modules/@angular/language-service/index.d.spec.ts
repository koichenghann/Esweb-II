import { TestBed, inject } from '@angular/core/testing';

import { IndexDService } from './index.d.service';

describe('IndexDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [IndexDService]
    });
  });

  it('should be created', inject([IndexDService], (service: IndexDService) => {
    expect(service).toBeTruthy();
  }));
});