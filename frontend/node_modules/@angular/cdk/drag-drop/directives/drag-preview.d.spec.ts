import { DragPreviewDDirective } from './drag-preview.d.directive';

describe('DragPreviewDDirective', () => {
  it('should create an instance', () => {
    const directive = new DragPreviewDDirective();
    expect(directive).toBeTruthy();
  });
});
