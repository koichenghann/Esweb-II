import { DropListDDirective } from './drop-list.d.directive';

describe('DropListDDirective', () => {
  it('should create an instance', () => {
    const directive = new DropListDDirective();
    expect(directive).toBeTruthy();
  });
});
