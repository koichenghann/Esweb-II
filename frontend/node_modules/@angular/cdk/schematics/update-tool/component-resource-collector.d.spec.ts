import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ComponentResourceCollectorDComponent } from './component-resource-collector.d.component';

describe('ComponentResourceCollectorDComponent', () => {
    let component: ComponentResourceCollectorDComponent;
    let fixture: ComponentFixture<ComponentResourceCollectorDComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [ComponentResourceCollectorDComponent]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(ComponentResourceCollectorDComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});