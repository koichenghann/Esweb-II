import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ComponentHarnessDComponent } from './component-harness.d.component';

describe('ComponentHarnessDComponent', () => {
    let component: ComponentHarnessDComponent;
    let fixture: ComponentFixture<ComponentHarnessDComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [ComponentHarnessDComponent]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(ComponentHarnessDComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});