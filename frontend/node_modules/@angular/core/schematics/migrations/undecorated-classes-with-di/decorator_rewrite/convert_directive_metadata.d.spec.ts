import { ConvertDirectiveMetadataDDirective } from './convert_directive_metadata.d.directive';

describe('ConvertDirectiveMetadataDDirective', () => {
  it('should create an instance', () => {
    const directive = new ConvertDirectiveMetadataDDirective();
    expect(directive).toBeTruthy();
  });
});
