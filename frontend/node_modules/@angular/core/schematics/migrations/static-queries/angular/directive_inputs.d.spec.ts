import { DirectiveInputsDDirective } from './directive_inputs.d.directive';

describe('DirectiveInputsDDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveInputsDDirective();
    expect(directive).toBeTruthy();
  });
});
