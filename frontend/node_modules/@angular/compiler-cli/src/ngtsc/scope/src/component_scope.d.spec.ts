import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ComponentScopeDComponent } from './component_scope.d.component';

describe('ComponentScopeDComponent', () => {
    let component: ComponentScopeDComponent;
    let fixture: ComponentFixture<ComponentScopeDComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [ComponentScopeDComponent]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(ComponentScopeDComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});