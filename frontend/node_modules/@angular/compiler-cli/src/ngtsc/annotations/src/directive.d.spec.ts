import { DirectiveDDirective } from './directive.d.directive';

describe('DirectiveDDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveDDirective();
    expect(directive).toBeTruthy();
  });
});
