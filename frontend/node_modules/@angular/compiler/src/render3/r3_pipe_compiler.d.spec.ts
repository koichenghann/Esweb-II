import { R3PipeCompilerDPipe } from './r3_pipe_compiler.d.pipe';

describe('R3PipeCompilerDPipe', () => {
  it('create an instance', () => {
    const pipe = new R3PipeCompilerDPipe();
    expect(pipe).toBeTruthy();
  });
});
