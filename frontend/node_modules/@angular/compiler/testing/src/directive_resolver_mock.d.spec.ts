import { DirectiveResolverMockDDirective } from './directive_resolver_mock.d.directive';

describe('DirectiveResolverMockDDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveResolverMockDDirective();
    expect(directive).toBeTruthy();
  });
});
