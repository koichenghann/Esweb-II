import { TestBed, async, inject } from '@angular/core/testing';

import { SchemaDGuard } from './schema.d.guard';

describe('SchemaDGuard', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SchemaDGuard]
    });
  });

  it('should ...', inject([SchemaDGuard], (guard: SchemaDGuard) => {
    expect(guard).toBeTruthy();
  }));
});

