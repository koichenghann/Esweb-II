import { SchemaDDirective } from './schema.d.directive';

describe('SchemaDDirective', () => {
  it('should create an instance', () => {
    const directive = new SchemaDDirective();
    expect(directive).toBeTruthy();
  });
});
