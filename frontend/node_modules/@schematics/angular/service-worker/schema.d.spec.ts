import { TestBed, inject } from '@angular/core/testing';

import { SchemaDService } from './schema.d.service';

describe('SchemaDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SchemaDService]
    });
  });

  it('should be created', inject([SchemaDService], (service: SchemaDService) => {
    expect(service).toBeTruthy();
  }));
});