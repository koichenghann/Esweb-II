import { TestBed, async, inject } from '@angular/core/testing';

import { NodeDGuard } from './node.d.guard';

describe('NodeDGuard', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [NodeDGuard]
    });
  });

  it('should ...', inject([NodeDGuard], (guard: NodeDGuard) => {
    expect(guard).toBeTruthy();
  }));
});

