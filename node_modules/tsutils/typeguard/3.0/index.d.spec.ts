import { TestBed, async, inject } from '@angular/core/testing';

import { IndexDGuard } from './index.d.guard';

describe('IndexDGuard', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [IndexDGuard]
    });
  });

  it('should ...', inject([IndexDGuard], (guard: IndexDGuard) => {
    expect(guard).toBeTruthy();
  }));
});

