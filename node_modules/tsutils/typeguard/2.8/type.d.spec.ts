import { TestBed, async, inject } from '@angular/core/testing';

import { TypeDGuard } from './type.d.guard';

describe('TypeDGuard', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TypeDGuard]
    });
  });

  it('should ...', inject([TypeDGuard], (guard: TypeDGuard) => {
    expect(guard).toBeTruthy();
  }));
});

