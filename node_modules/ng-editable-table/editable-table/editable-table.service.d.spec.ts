import { TestBed, inject } from '@angular/core/testing';

import { EditableTableDService } from './editable-table.d.service';

describe('EditableTableDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [EditableTableDService]
    });
  });

  it('should be created', inject([EditableTableDService], (service: EditableTableDService) => {
    expect(service).toBeTruthy();
  }));
});