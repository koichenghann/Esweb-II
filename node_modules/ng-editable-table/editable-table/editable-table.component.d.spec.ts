import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { EditableTableDComponent } from './editable-table.d.component';

describe('EditableTableDComponent', () => {
    let component: EditableTableDComponent;
    let fixture: ComponentFixture<EditableTableDComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [EditableTableDComponent]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(EditableTableDComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});