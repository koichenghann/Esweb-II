import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TestingDComponent } from './testing.d.component';

describe('TestingDComponent', () => {
    let component: TestingDComponent;
    let fixture: ComponentFixture<TestingDComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [TestingDComponent]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(TestingDComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});