import { RouterOutletDDirective } from './router_outlet.d.directive';

describe('RouterOutletDDirective', () => {
  it('should create an instance', () => {
    const directive = new RouterOutletDDirective();
    expect(directive).toBeTruthy();
  });
});
