import { RouterLinkActiveDDirective } from './router_link_active.d.directive';

describe('RouterLinkActiveDDirective', () => {
  it('should create an instance', () => {
    const directive = new RouterLinkActiveDDirective();
    expect(directive).toBeTruthy();
  });
});
