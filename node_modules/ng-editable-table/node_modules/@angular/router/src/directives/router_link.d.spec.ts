import { RouterLinkDDirective } from './router_link.d.directive';

describe('RouterLinkDDirective', () => {
  it('should create an instance', () => {
    const directive = new RouterLinkDDirective();
    expect(directive).toBeTruthy();
  });
});
