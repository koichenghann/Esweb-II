import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ComponentFixtureDComponent } from './component_fixture.d.component';

describe('ComponentFixtureDComponent', () => {
    let component: ComponentFixtureDComponent;
    let fixture: ComponentFixture<ComponentFixtureDComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [ComponentFixtureDComponent]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(ComponentFixtureDComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});