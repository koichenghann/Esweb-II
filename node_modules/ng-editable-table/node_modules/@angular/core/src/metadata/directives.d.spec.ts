import { DirectivesDDirective } from './directives.d.directive';

describe('DirectivesDDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivesDDirective();
    expect(directive).toBeTruthy();
  });
});
