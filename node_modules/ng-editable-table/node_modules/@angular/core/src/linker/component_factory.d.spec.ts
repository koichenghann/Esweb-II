import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ComponentFactoryDComponent } from './component_factory.d.component';

describe('ComponentFactoryDComponent', () => {
    let component: ComponentFactoryDComponent;
    let fixture: ComponentFixture<ComponentFactoryDComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [ComponentFactoryDComponent]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(ComponentFactoryDComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});