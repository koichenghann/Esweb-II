import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ComponentFactoryResolverDComponent } from './component_factory_resolver.d.component';

describe('ComponentFactoryResolverDComponent', () => {
    let component: ComponentFactoryResolverDComponent;
    let fixture: ComponentFixture<ComponentFactoryResolverDComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [ComponentFactoryResolverDComponent]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(ComponentFactoryResolverDComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});