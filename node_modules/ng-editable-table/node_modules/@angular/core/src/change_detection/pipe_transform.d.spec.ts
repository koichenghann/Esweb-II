import { PipeTransformDPipe } from './pipe_transform.d.pipe';

describe('PipeTransformDPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTransformDPipe();
    expect(pipe).toBeTruthy();
  });
});
