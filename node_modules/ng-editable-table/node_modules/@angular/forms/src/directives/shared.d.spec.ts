import { SharedDDirective } from './shared.d.directive';

describe('SharedDDirective', () => {
  it('should create an instance', () => {
    const directive = new SharedDDirective();
    expect(directive).toBeTruthy();
  });
});
