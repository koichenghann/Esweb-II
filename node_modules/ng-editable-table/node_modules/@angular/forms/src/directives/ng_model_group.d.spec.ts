import { NgModelGroupDDirective } from './ng_model_group.d.directive';

describe('NgModelGroupDDirective', () => {
  it('should create an instance', () => {
    const directive = new NgModelGroupDDirective();
    expect(directive).toBeTruthy();
  });
});
