import { NgModelDDirective } from './ng_model.d.directive';

describe('NgModelDDirective', () => {
  it('should create an instance', () => {
    const directive = new NgModelDDirective();
    expect(directive).toBeTruthy();
  });
});
