import { FormGroupNameDDirective } from './form_group_name.d.directive';

describe('FormGroupNameDDirective', () => {
  it('should create an instance', () => {
    const directive = new FormGroupNameDDirective();
    expect(directive).toBeTruthy();
  });
});
