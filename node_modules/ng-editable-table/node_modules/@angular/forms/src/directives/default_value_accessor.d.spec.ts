import { DefaultValueAccessorDDirective } from './default_value_accessor.d.directive';

describe('DefaultValueAccessorDDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultValueAccessorDDirective();
    expect(directive).toBeTruthy();
  });
});
