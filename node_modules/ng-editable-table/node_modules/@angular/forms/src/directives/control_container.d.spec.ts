import { ControlContainerDDirective } from './control_container.d.directive';

describe('ControlContainerDDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlContainerDDirective();
    expect(directive).toBeTruthy();
  });
});
