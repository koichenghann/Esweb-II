import { ReactiveErrorsDDirective } from './reactive_errors.d.directive';

describe('ReactiveErrorsDDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactiveErrorsDDirective();
    expect(directive).toBeTruthy();
  });
});
