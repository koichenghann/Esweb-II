import { TemplateDrivenErrorsDDirective } from './template_driven_errors.d.directive';

describe('TemplateDrivenErrorsDDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateDrivenErrorsDDirective();
    expect(directive).toBeTruthy();
  });
});
