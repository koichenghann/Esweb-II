import { DirectiveNormalizerDDirective } from './directive_normalizer.d.directive';

describe('DirectiveNormalizerDDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveNormalizerDDirective();
    expect(directive).toBeTruthy();
  });
});
