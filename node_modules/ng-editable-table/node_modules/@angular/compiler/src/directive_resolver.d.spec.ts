import { DirectiveResolverDDirective } from './directive_resolver.d.directive';

describe('DirectiveResolverDDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveResolverDDirective();
    expect(directive).toBeTruthy();
  });
});
