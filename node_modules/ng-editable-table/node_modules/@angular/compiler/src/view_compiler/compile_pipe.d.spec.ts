import { CompilePipeDPipe } from './compile_pipe.d.pipe';

describe('CompilePipeDPipe', () => {
  it('create an instance', () => {
    const pipe = new CompilePipeDPipe();
    expect(pipe).toBeTruthy();
  });
});
