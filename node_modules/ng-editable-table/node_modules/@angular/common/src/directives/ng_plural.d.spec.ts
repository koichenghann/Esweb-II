import { NgPluralDDirective } from './ng_plural.d.directive';

describe('NgPluralDDirective', () => {
  it('should create an instance', () => {
    const directive = new NgPluralDDirective();
    expect(directive).toBeTruthy();
  });
});
