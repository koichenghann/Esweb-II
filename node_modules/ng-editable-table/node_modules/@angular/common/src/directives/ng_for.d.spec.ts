import { NgForDDirective } from './ng_for.d.directive';

describe('NgForDDirective', () => {
  it('should create an instance', () => {
    const directive = new NgForDDirective();
    expect(directive).toBeTruthy();
  });
});
