import { NgIfDDirective } from './ng_if.d.directive';

describe('NgIfDDirective', () => {
  it('should create an instance', () => {
    const directive = new NgIfDDirective();
    expect(directive).toBeTruthy();
  });
});
