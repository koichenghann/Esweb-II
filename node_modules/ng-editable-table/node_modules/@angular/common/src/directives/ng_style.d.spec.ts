import { NgStyleDDirective } from './ng_style.d.directive';

describe('NgStyleDDirective', () => {
  it('should create an instance', () => {
    const directive = new NgStyleDDirective();
    expect(directive).toBeTruthy();
  });
});
