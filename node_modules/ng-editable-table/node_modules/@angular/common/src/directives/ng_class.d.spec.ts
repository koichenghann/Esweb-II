import { NgClassDDirective } from './ng_class.d.directive';

describe('NgClassDDirective', () => {
  it('should create an instance', () => {
    const directive = new NgClassDDirective();
    expect(directive).toBeTruthy();
  });
});
