import { NgSwitchDDirective } from './ng_switch.d.directive';

describe('NgSwitchDDirective', () => {
  it('should create an instance', () => {
    const directive = new NgSwitchDDirective();
    expect(directive).toBeTruthy();
  });
});
