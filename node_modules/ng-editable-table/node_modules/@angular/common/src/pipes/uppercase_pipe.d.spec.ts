import { UppercasePipeDPipe } from './uppercase_pipe.d.pipe';

describe('UppercasePipeDPipe', () => {
  it('create an instance', () => {
    const pipe = new UppercasePipeDPipe();
    expect(pipe).toBeTruthy();
  });
});
