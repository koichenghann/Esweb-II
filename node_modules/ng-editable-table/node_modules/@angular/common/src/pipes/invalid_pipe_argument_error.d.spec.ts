import { InvalidPipeArgumentErrorDPipe } from './invalid_pipe_argument_error.d.pipe';

describe('InvalidPipeArgumentErrorDPipe', () => {
  it('create an instance', () => {
    const pipe = new InvalidPipeArgumentErrorDPipe();
    expect(pipe).toBeTruthy();
  });
});
