import { SlicePipeDPipe } from './slice_pipe.d.pipe';

describe('SlicePipeDPipe', () => {
  it('create an instance', () => {
    const pipe = new SlicePipeDPipe();
    expect(pipe).toBeTruthy();
  });
});
