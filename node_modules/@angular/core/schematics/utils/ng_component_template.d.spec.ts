import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { NgComponentTemplateDComponent } from './ng_component_template.d.component';

describe('NgComponentTemplateDComponent', () => {
    let component: NgComponentTemplateDComponent;
    let fixture: ComponentFixture<NgComponentTemplateDComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [NgComponentTemplateDComponent]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(NgComponentTemplateDComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});