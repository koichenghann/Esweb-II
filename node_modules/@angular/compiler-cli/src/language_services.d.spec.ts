import { TestBed, inject } from '@angular/core/testing';

import { LanguageServicesDService } from './language_services.d.service';

describe('LanguageServicesDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [LanguageServicesDService]
    });
  });

  it('should be created', inject([LanguageServicesDService], (service: LanguageServicesDService) => {
    expect(service).toBeTruthy();
  }));
});