import { TestBed, inject } from '@angular/core/testing';

import { ExpressionTypeDService } from './expression_type.d.service';

describe('ExpressionTypeDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ExpressionTypeDService]
    });
  });

  it('should be created', inject([ExpressionTypeDService], (service: ExpressionTypeDService) => {
    expect(service).toBeTruthy();
  }));
});