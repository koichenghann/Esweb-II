import { TestBed, inject } from '@angular/core/testing';

import { GlobalSymbolsDService } from './global_symbols.d.service';

describe('GlobalSymbolsDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [GlobalSymbolsDService]
    });
  });

  it('should be created', inject([GlobalSymbolsDService], (service: GlobalSymbolsDService) => {
    expect(service).toBeTruthy();
  }));
});