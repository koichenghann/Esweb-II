import { TestBed, inject } from '@angular/core/testing';

import { TypescriptHostDService } from './typescript_host.d.service';

describe('TypescriptHostDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TypescriptHostDService]
    });
  });

  it('should be created', inject([TypescriptHostDService], (service: TypescriptHostDService) => {
    expect(service).toBeTruthy();
  }));
});