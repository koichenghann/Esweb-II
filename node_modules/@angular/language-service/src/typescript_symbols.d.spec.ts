import { TestBed, inject } from '@angular/core/testing';

import { TypescriptSymbolsDService } from './typescript_symbols.d.service';

describe('TypescriptSymbolsDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TypescriptSymbolsDService]
    });
  });

  it('should be created', inject([TypescriptSymbolsDService], (service: TypescriptSymbolsDService) => {
    expect(service).toBeTruthy();
  }));
});