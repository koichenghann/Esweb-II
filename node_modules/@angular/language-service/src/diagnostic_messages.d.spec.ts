import { TestBed, inject } from '@angular/core/testing';

import { DiagnosticMessagesDService } from './diagnostic_messages.d.service';

describe('DiagnosticMessagesDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DiagnosticMessagesDService]
    });
  });

  it('should be created', inject([DiagnosticMessagesDService], (service: DiagnosticMessagesDService) => {
    expect(service).toBeTruthy();
  }));
});