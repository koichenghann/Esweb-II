import { TestBed, inject } from '@angular/core/testing';

import { TypesDService } from './types.d.service';

describe('TypesDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TypesDService]
    });
  });

  it('should be created', inject([TypesDService], (service: TypesDService) => {
    expect(service).toBeTruthy();
  }));
});