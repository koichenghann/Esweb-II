import { TestBed, inject } from '@angular/core/testing';

import { HoverDService } from './hover.d.service';

describe('HoverDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [HoverDService]
    });
  });

  it('should be created', inject([HoverDService], (service: HoverDService) => {
    expect(service).toBeTruthy();
  }));
});