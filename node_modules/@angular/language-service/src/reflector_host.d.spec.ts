import { TestBed, inject } from '@angular/core/testing';

import { ReflectorHostDService } from './reflector_host.d.service';

describe('ReflectorHostDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ReflectorHostDService]
    });
  });

  it('should be created', inject([ReflectorHostDService], (service: ReflectorHostDService) => {
    expect(service).toBeTruthy();
  }));
});