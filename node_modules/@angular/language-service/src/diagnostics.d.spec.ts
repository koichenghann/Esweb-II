import { TestBed, inject } from '@angular/core/testing';

import { DiagnosticsDService } from './diagnostics.d.service';

describe('DiagnosticsDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DiagnosticsDService]
    });
  });

  it('should be created', inject([DiagnosticsDService], (service: DiagnosticsDService) => {
    expect(service).toBeTruthy();
  }));
});