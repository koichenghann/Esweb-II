import { TestBed, inject } from '@angular/core/testing';

import { CompletionsDService } from './completions.d.service';

describe('CompletionsDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CompletionsDService]
    });
  });

  it('should be created', inject([CompletionsDService], (service: CompletionsDService) => {
    expect(service).toBeTruthy();
  }));
});