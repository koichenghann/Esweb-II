import { TestBed, inject } from '@angular/core/testing';

import { TemplateDService } from './template.d.service';

describe('TemplateDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TemplateDService]
    });
  });

  it('should be created', inject([TemplateDService], (service: TemplateDService) => {
    expect(service).toBeTruthy();
  }));
});