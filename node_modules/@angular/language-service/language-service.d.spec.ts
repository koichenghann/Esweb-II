import { TestBed, inject } from '@angular/core/testing';

import { LanguageServiceDService } from './language-service.d.service';

describe('LanguageServiceDService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [LanguageServiceDService]
    });
  });

  it('should be created', inject([LanguageServiceDService], (service: LanguageServiceDService) => {
    expect(service).toBeTruthy();
  }));
});