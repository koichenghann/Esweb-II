import { DialogContentDirectivesDDirective } from './dialog-content-directives.d.directive';

describe('DialogContentDirectivesDDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogContentDirectivesDDirective();
    expect(directive).toBeTruthy();
  });
});
