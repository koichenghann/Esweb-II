import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { BuildComponentDComponent } from './build-component.d.component';

describe('BuildComponentDComponent', () => {
    let component: BuildComponentDComponent;
    let fixture: ComponentFixture<BuildComponentDComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [BuildComponentDComponent]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(BuildComponentDComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});