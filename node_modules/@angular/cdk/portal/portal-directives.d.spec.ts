import { PortalDirectivesDDirective } from './portal-directives.d.directive';

describe('PortalDirectivesDDirective', () => {
  it('should create an instance', () => {
    const directive = new PortalDirectivesDDirective();
    expect(directive).toBeTruthy();
  });
});
