import { ConfigDDirective } from './config.d.directive';

describe('ConfigDDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfigDDirective();
    expect(directive).toBeTruthy();
  });
});
