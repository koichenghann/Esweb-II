import { DragPlaceholderDDirective } from './drag-placeholder.d.directive';

describe('DragPlaceholderDDirective', () => {
  it('should create an instance', () => {
    const directive = new DragPlaceholderDDirective();
    expect(directive).toBeTruthy();
  });
});
