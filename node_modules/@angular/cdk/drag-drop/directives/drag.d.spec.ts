import { DragDDirective } from './drag.d.directive';

describe('DragDDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDDirective();
    expect(directive).toBeTruthy();
  });
});
