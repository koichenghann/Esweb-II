import { DropListGroupDDirective } from './drop-list-group.d.directive';

describe('DropListGroupDDirective', () => {
  it('should create an instance', () => {
    const directive = new DropListGroupDDirective();
    expect(directive).toBeTruthy();
  });
});
