import { DragHandleDDirective } from './drag-handle.d.directive';

describe('DragHandleDDirective', () => {
  it('should create an instance', () => {
    const directive = new DragHandleDDirective();
    expect(directive).toBeTruthy();
  });
});
