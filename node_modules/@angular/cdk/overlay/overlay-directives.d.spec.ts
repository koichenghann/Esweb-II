import { OverlayDirectivesDDirective } from './overlay-directives.d.directive';

describe('OverlayDirectivesDDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayDirectivesDDirective();
    expect(directive).toBeTruthy();
  });
});
