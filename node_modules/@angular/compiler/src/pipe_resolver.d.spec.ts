import { PipeResolverDPipe } from './pipe_resolver.d.pipe';

describe('PipeResolverDPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeResolverDPipe();
    expect(pipe).toBeTruthy();
  });
});
