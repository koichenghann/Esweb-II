import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ComponentMaxInlineDeclarationsRuleDComponent } from './componentMaxInlineDeclarationsRule.d.component';

describe('ComponentMaxInlineDeclarationsRuleDComponent', () => {
    let component: ComponentMaxInlineDeclarationsRuleDComponent;
    let fixture: ComponentFixture<ComponentMaxInlineDeclarationsRuleDComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [ComponentMaxInlineDeclarationsRuleDComponent]
        })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(ComponentMaxInlineDeclarationsRuleDComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});